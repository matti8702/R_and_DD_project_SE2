\subsection{Overview: high-level components and interactions}
This section presents the high-level structure of the system, which will be explained in detail in the following subsections. 
The image shows the \textbf{3-tier architecture} of the system.

\begin{figure}[H]
    \centering
    \includegraphics[scale = 0.65]{Architectural_design/other_diagrams/overview_diagram.pdf}
    \caption{Overview of the system architecture.}
\end{figure}
\subsection{Component view}
\label{section2.2}
This section presents the Component view diagram. It outlines the system's main building blocks, their responsibilities, and how they interact.

\begin{figure}[H]
    \centering
    \includegraphics[scale = 0.31]{Architectural_design/uml_diagrams/component_diagram.pdf}
    \caption{Component diagram of the system.}
\end{figure}

\noindent
Each component is explained in detail:
\begin{itemize}
    \item \textbf{Web App}: manages the layout of the website accessed by the users.
    \item \textbf{Controller}: serves as the entry point of the application server, receiving requests from users' web apps and handling them through calls to other system components.
    \item \textbf{Notification Manager}: handles real-time notifications, displaying updates relevant to the user while they interact with the website.
    \item \textbf{Registration Manager}: manages the registration of new users (students, company members, and university members) as well as universities and companies.
    \item \textbf{Login Manager}: oversees user authentication by verifying credentials and generating tokens. These tokens enable user recognition during the requests and are included in every call from the web app.
    \item \textbf{CV Manager}: handles student CVs, both in file format and as digitalized versions.
    \item \textbf{Internship Proposal Manager}: focuses on managing internship proposals published by company members, excluding data related to ongoing internships.
    \item \textbf{Match Manager}: oversees the lifecycle of match proposals, from creation to the communication of final outcomes.
    \item \textbf{Chat Manager}: enables communication between company members and students within the same selection phase through a dedicated chat.
    \item \textbf{Selection Manager}: manages the entire selection process for internship proposals, from scheduling interviews to sharing results with students.
    \item \textbf{Internship Status Manager}: handles all aspects of ongoing internships, including reports, complaints, and feedback.
    \item \textbf{Suggestion Manager}: detects suggestions related to digitalized CVs and internship descriptions.
    \item \textbf{Email Manager}: manages communication with the external email service in order to send emails to registered users.
    \item \textbf{Recommendation Manager}: works with the external statistical analysis tool in order to generate recommendations for students and companies.
    \item \textbf{Interview Scheduler}: coordinates with the external video conferencing tool to schedule interviews, generate meeting links, and facilitate virtual sessions for online interviews.
    \item \textbf{Data Manager}: interacts with the Relational DBMS to perform database operations requested by other components.
\end{itemize}
\subsection{Deployment view}
The Deployment view section illustrates the distribution of system modules across the physical infrastructure, showing how they interact with each other to ensure efficient operation and scalability.

\begin{figure}[H]
    \centering
    \includegraphics[scale = 0.35]{Architectural_design/uml_diagrams/deployment_diagram.png}
    \caption{Deployment diagram of the system.}
\end{figure}

\noindent
Each node is explained in detail:
\begin{itemize}
    \item \textbf{User Device}: the user's device runs a \textbf{Browser} that interacts with the system through HTTPS or WebSocket protocols.
    \item \textbf{Perimeter Firewall}: it acts as a security layer between the user device and the internal system, filtering traffic.
    \item \textbf{Load Balancer}: the \textbf{Load Balancer system} distributes incoming traffic between multiple application servers (including the main server and its replicas) using HTTPS or WebSocket protocols to ensure high availability.
    \item \textbf{Application Servers}: it runs the core application logic, using replicas.
    \item \textbf{Database Cluster}: the \textbf{Database Management System (DBMS)} manages the primary and replica databases (\textbf{Primary DB}, \textbf{Replica 1}, \textbf{Replica 2}). The DBMS handles SQL queries and ensures data replication across the databases.
    \item \textbf{Internal Firewall}: it protects the communication between the application servers and the DBMS, managing the flow of TCP/IP and SQL queries.
\end{itemize}
\subsection{Component interfaces}
This section lists all the functions exposed by the interfaces of the architectural components. These functions will be used in the runtime views presented in the next sub section.
%TODO: review!!
\begin{itemize}
    \item \textbf{Controller: RestAPI interface}
    \begin{itemize}
        \item getAllUniversitiesNames()
        \item getAllCompaniesNames()
        \item registerAsUniversityMember(name, surname, dateOfBirth, password, emailAddress, universityName)
        \item registerAsCompanyMember(name, surname, dateOfBirth, password, emailAddress, companyName)
        \item registerAsStudent(name, surname, dateOfBirth, password, emailAddress, universityName)
        \item registerUniversity(userInfo, universityName)
        \item registerCompany(userInfo, companyName, companyDescription)
        \item confirmEmailAddress(emailAddress)
        \item login(emailAddress, password)
        \item publishInternship(name, applicationStartDate, applicationEndDate, maxStudentAllowed, description, requirements, domain, tags, terms)
        \item modifyInternship(internshipID, internshipData)
        \item deleteInternship(internshipID)
        \item getInternshipsByKeywords(keywords)
        \item modifySectionDescription(text, section)
        \item publishCV(CVfile)
        \item getCV(studentID)
        \item modifyInternshipDetails(internshipDetails)
        \item getInternshipDetails(internshipID)
        \item studentMatchProposal(studentID, internshipID)
        \item companyMemberMatchProposal(studentID, internshipID)
        \item evaluateMatchProposal(matchProposal, choice)
        \item submitMeeting(form)
        \item getCandidates(internshipID)
        \item getStudentInfo(studentID)
        \item addAdmissionChoice(result)
        \item addDecision(choice)
        \item sendMessage(message)
        \item submitReport(report)
        \item submitComplaint(complaint)
        \item getStudents(universityName)
        \item interruptInternship(internshipID, studentID)
        \item postFeedback(feedback)
    \end{itemize}
    \item \textbf{Notification Manager: Notification interface}
    \begin{itemize}
        \item sendNotifyNewMatch(matchProposal)
        \item sendNotifyMatchAccepted(matchProposal)
        \item sendNotifyMatchDeclined(matchProposal)
        \item sendNotifyMeeting(meeting)
        \item sendNotifyContract(contract)
        \item sendNotifyRejection()
        \item notifyRemoveChat(internshipID, studentID)
        \item sendNotifyMessage(message)
        \item notifyReport(report)
        \item notifyNewComplaint(complaint)
        \item notifyInternshipInterruption(internshipID, studentID)
    \end{itemize}
    \item \textbf{Registration Manager: Registration interface}
    \begin{itemize}
        \item registerAsUniversityMember(name, surname, dateOfBirth, password, emailAddress, universityName)
        \item registerAsCompanyMember(name, surname, dateOfBirth, password, emailAddress, companyName)
        \item registerAsStudent(name, surname, dateOfBirth, password, emailAddress, universityName)
        \item confirmEmailAddress(emailAddress)
        \item registerUniversity(userInfo, universityName)
        \item registerCompany(userInfo, companyName, companyDescription)
    \end{itemize}
    \item \textbf{Login Manager: Login interface}
    \begin{itemize}
        \item login(emailAddress, password)
    \end{itemize}
    \item \textbf{CV Manager: CV interface}
    \begin{itemize}
        \item modifySectionDescription(text, section)
        \item publishCV(CVfile)
        \item getCV(studentID)
    \end{itemize}
    \item \textbf{Internship Proposal Manager: Proposal interface}
    \begin{itemize}
        \item publishInternship(internshipData)
        \item modifyInternship(internshipID, internshipData)
        \item deleteInternship(internshipID)
        \item getInternshipsByKeywords(keywords)
        \item modifyInternshipDetails(internshipDetails)
        \item getInternshipDetails(internshipID)
    \end{itemize}
    \item \textbf{Match Manager: Match interface}
    \begin{itemize}
        \item studentMatchProposal(studentID, internshipID)
        \item checkAlreadyExistentMatch(studentID,internshipID)
        \item companyMemberMatchProposal(studentID, internshipID)
        \item evaluateMatchProposal(matchProposal, choice)
    \end{itemize}
    \item \textbf{Chat Manager: Chat interface}
    \begin{itemize}
        \item createNewChat()
        \item deleteChat(internshipID, studentID)
        \item sendMessage(message)
    \end{itemize}
    \item \textbf{Selection Manager: Selection interface}
    \begin{itemize}
        \item submitMeeting(form)
        \item getCandidates(internshipID)
        \item getStudentInfo(studentID)
        \item addAdmissionChoice(result)
        \item addDecision(choice)
    \end{itemize}
    \item \textbf{Internship Status Manager: Internship status interface}
    \begin{itemize}
        \item submitReport(report)
        \item submitComplaint(complaint)
        \item getStudents(universityName)
        \item interruptInternship(internshipID, studentID)
        \item postFeedback(feedback)
    \end{itemize}
    \item \textbf{Suggestion Manager: Suggestion interface}
    \begin{itemize}
        \item getCVSuggestions(digitalizedCvSection)
        \item getInternshipSuggestion(internshipDetails)
    \end{itemize}
    \item \textbf{Email Manager: Email interface}
    \begin{itemize}
        \item sendConfirmationEmail(emailAddress)
        \item sendRecommendationEmail(users)
    \end{itemize}
    \item \textbf{Recommendation Manager: Recommendation interface}
    \begin{itemize}
        \item addCompany(companyID)
        \item deleteCompany(companyID)
        \item addStudent(studentID)
        \item deleteStudent(studentID)
        \item getStudentRecommendations(studentID)
        \item getCompanyRecommendations(companyID)
        \item addInternship(companyID, internshipID, tags)
        \item updateInternshipTags(companyID, internshipID, tagsRemoved, tagsAdded)
        \item deleteInternship(companyID, internshipID)
        \item updateCV(digitalizedCV)
        \item updateScores(internshipId, internshipTags, studentTags, grades)
    \end{itemize}
    \item \textbf{Interview Scheduler: Scheduler interface}
    \begin{itemize}
        \item submitMeeting(form)
        \item attachWarning(meeting, overlappingEvents)
    \end{itemize}
    \item \textbf{Data Manager: Data interface}
    \begin{itemize}
        \item getAllUniversitiesNames()
        \item getAllCompaniesNames()
        \item getUniversityInfo(universityName)
        \item getCompanyInfo(companyName)
        \item getUsersEmail()
        \item addUniversityMember(name, surname, dateOfBirth, password, emailAddress, universityName)
        \item addUniversity(universityName, emailDomain)
        \item addCompanyMember(name, surname, dateOfBirth, password, emailAddress, companyName)
        \item addCompany(companyName, companyDescription, emailDomain)
        \item addStudent(name, surname, dateOfBirth, password, emailAddress, universityName)
        \item confirmUserRegistration(emailAddress)
        \item cancelUser(emailAddress)
        \item cancelUniversity(emailAddress)
        \item cancelCompany(emailAddress)
        \item getUserPassword(emailAddress)
        \item addToken(userEmail, token)
        \item getUserStatus(userEmail)
        \item getStudentID(userEmail)
        \item getCompanyIDByMember(companyMemberEmailAddress)
        \item updateRecommendations(studentID, recommendations)
        \item updateRecommendations(companyID, recommendations)
        \item addInternshipProposal(internshipData)
        \item deleteInternshipProposal(internshipID)
        \item getInternshipsByKeywords(keywords)
        \item modifyCV(text, section)
        \item saveCVPath(CVPath)
        \item newRecommendation(studentID, newRecommendation)
        \item newRecommendation(companyID, newRecommendation)
        \item modifyInternshipDetails(internshipID, internshipDetails)
        \item getInternshipDetails(internshipID)
        \item getCV(studentID)
        \item addMatchProposal(matchProposal)
        \item getMatchCount(studentID, InternshipID)
        \item updateMatchProposal(matchProposal, choice)
        \item addMeeting(meeting)
        \item findOverlappingEvents(meeting)
        \item getCandidates(internshipID)
        \item getStudentInfo(studentID)
        \item removeCandidate(studentID)
        \item getInternshipContract(internshipID)
        \item deleteChat(internshipID, studentID)
        \item addMessage(message)
        \item addReport(report)
        \item addComplaint(complaint)
        \item getStudents(universityName)
        \item interruptInternship(internshipID, studentID)
        \item closeInternship(internshipID)
        \item getCompanyIDByInternship(internshipID)
    \end{itemize}
\end{itemize}
\subsection{Runtime views}
This section presents the runtime views for the use cases defined in the RASD (one view for each use case). These views are shown as sequence diagrams. Unlike those provided in the RASD, these diagrams use actual architectural components as "actors," as previously defined in \underline{\hyperref[section2.2]{section 2.2}}.
%TODO: add diagrams descriptions
\runtimeview
{Registration of a university member}
{
    This sequence diagram illustrates the registration process for university members. It also includes the registration of the member's university if it has not yet been added to the system. The user has one day to confirm the email address provided during registration, following the instructions sent in the system-generated email. Once confirmed, the system activates the user account and the created university, if applicable. Note that, during registration, both the user and the newly created university (if any) are added to the system (by calling \textit{addUniversityMember} and \textit{addUniversity} functions) with a flag "notConfirmed", indicating that the email address has not been confirmed. Until the email is confirmed, the user cannot access the platform. Finally, the \textit{confirmUserRegistration} function activates the user and also the university (in case the user has registered it).
}
{0.14}

\runtimeview
{Registration of a company member}
{
    This sequence diagram illustrates the registration process for company members. It also includes the registration of the member's company if it has not yet been added to the system. The user has one day to confirm the email address provided during registration, following the instructions sent in the system-generated email. Once confirmed, the system activates the user account and the created company, if applicable. Note that, during registration, both the user and the newly created company (if any) are added to the system (by calling \textit{addCompanyMember} and \textit{addCompany} functions) with a flag "notConfirmed", indicating that the email address has not been confirmed. Until the email is confirmed, the user cannot access the platform. In case of a new company is created, the system will send the generated ID of the newly created company to the external statistical analysis tool. If the user does not confirm the email address within one day of registration, the system will notify the tool again in order to delete the company's ID. Moreover, the \textit{confirmUserRegistration} function activates the user and also the university (in case the user has registered it). Finally, the \textit{ENTITYTYPE} enumeration allows specifying the type of entity on which an operation is being performed (modification, insertion, deletion, or reading) on the external statistical analysis tool. The enumeration can take three values: \textit{STUDENT}, \textit{COMPANY} or \textit{INTERNSHIP}.
}
{0.12}

\runtimeview
{Registration of a student}
{
    This sequence diagram illustrates the registration process for students. The user has one day to confirm the email address provided during registration, following the instructions sent in the system-generated email. Once confirmed, the system activates the user account. Note that, during registration, the user is added to the system (by calling \textit{addStudent} function) with a flag "notConfirmed", indicating that the email address has not been confirmed. Until the email is confirmed, the user cannot access the platform. The system will send the generated ID of the newly student to the external statistical analysis tool. If the user does not confirm the email address within one day of registration, the system will notify the tool again in order to delete the student's ID.
}
{0.13}

\runtimeview
{Login of a university member}
{
    
//specify that checkCredentials() check also if user has already confirmed the email address
//describe token system

}
{0.28}

\runtimeview
{Login of student}
{
    
//specify that checkCredentials() check also if user has already confirmed the email address
//describe token system
}
{0.19}

\runtimeview
{Login of company member}
{
    //specify that checkCredentials() check also if user has already confirmed the email address
//describe token system
}
{0.19}

\runtimeview
{Creation of an internship}
{
    This diagram shows the creation of an internship proposal by the company member. The system adds the proposal to the database and notifies the external statistical analysis tool of the addition. The tool will receive the ID of the created internship and the ID of the member's company. The tool must know the company that published the internship, as the recommendations will be directed to companies (and thus notified to all company members) and students.
}
{0.19}

\runtimeview
{Modify an internship}
{
    This diagram shows the modification of an internship proposal made by a company member. The system saves all the changes to the database and communicates the changes regarding only the tags to the external statistical analysis tool, specifying the tags that were removed (\textit{tagsRemoved}) and the tags that were added (\textit{tagsAdded}).
}
{0.17}

\runtimeview
{Delete an internship}
{
    This diagram shows the deletion of an internship proposal made by a company member. The system deletes the proposal from the database and communicates the deletion to the external statistical analysis tool, specifying the type of entity to be deleted (\textit{ENTITYTYPE.INTERNSHIP}), the company ID, and the internship ID.
}
{0.23}

\runtimeview
{Searching for an internship by means of keywords}
{
    This diagram shows the search for internship proposals performed by students through the insertion of keywords. The system searches for all internship proposals that have a name similar to the one specified by the user and, if found, sorts them based on the recommendations received from the external statistical analysis tool.
}
{0.22}

\runtimeview
{Modify the digitalized curriculum vitae version}
{}
{0.18}

\runtimeview
{Upload a new CV document}
{}
{0.28}

\runtimeview
{Notify new recommendations}
{}
{0.28}

\runtimeview
{Apply a suggestion to the digitalized curriculum vitae}
{}
{0.18}

\runtimeview
{Apply a suggestion to the internship description}
{}
{0.16}

\runtimeview
{Student sends a match proposal}
{}
{0.18}

\runtimeview
{Company member sends a match proposal}
{}
{0.18}

\runtimeview
{Student responds to company's match proposal}
{}
{0.18}

\runtimeview
{Company member responds to student's match proposal}
{}
{0.16}

\runtimeview
{Company member schedules the meeting}
{}
{0.13}

\runtimeview
{Company member communicates the result of the interview}
{}
{0.19}

\runtimeview
{Student decides on the offer}
{}
{0.23}

\runtimeview
{User writes a message}
{}
{0.22}

\runtimeview
{Student writes a report}
{}
{0.19}

\runtimeview
{Company member writes a report}
{}
{0.19}

\runtimeview
{Student writes a complaint}
{}
{0.19}

\runtimeview
{University member interrupts an internship}
{}
{0.15}

\runtimeview
{Student or company member posts a feedback}
{}
{0.21}

\runtimeview
{The system closes the internship}
{}
{0.29}

\subsection{Selected architectural styles and patterns}
%TODO: 3-tier, MVC, ...
\subsection{Other design decisions}
%TODO: relational db, tokens?